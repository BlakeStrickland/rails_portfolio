<%= form_for(@my_info) do |f| %>
  <% if @my_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_info.errors.count, "error") %> prohibited this my_info from being saved:</h2>

      <ul>
      <% @my_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
